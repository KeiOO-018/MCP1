# 학습 목록 — 엔진 소스 판독 14건

**이 파일은 포트폴리오가 아니다. 면접 전에 읽어야 할 목록이다.**

[p06-07-엔진소스-판독.md](p06-07-엔진소스-판독.md)에 *"인용 위치는 재확인 가능하며 아래 다섯은
설명할 수 있다"*고 적었다. **그 문장은 지금 시점에 아직 참이 아니다.** 이 14건을 실제로 읽고
설명할 수 있게 되면 참이 되고, 안 읽으면 기술 면접에서 질문 하나로 무너진다.

우선순위 — **★ 다섯을 먼저**(포트폴리오에 실린 것) → 나머지 아홉.
엔진 소스 위치: `C:\Program Files\Epic Games\UE_5.8\Engine\Source`

---

## ★ 포트폴리오에 실린 다섯

### A6 ★ UMG를 만들 수 없다는 근거
`Runtime/UMG/Public/Blueprint/WidgetBlueprintGeneratedClass.h:90-92` · `Runtime/Engine/Private/HUD.cpp:175-177, 642-656`

- 설명할 것 — `UPROPERTY` 지정자가 무엇이고 `DuplicateTransient`가 무슨 뜻인지. Edit 플래그가 없으면 왜 외부 도구가 못 건드리는지. `ReceiveDrawHUD`가 언제 호출되는지
- 예상 질문 — *"UMG 대신 HUD 캔버스를 쓰면 뭘 잃습니까?"* (레이아웃·애니메이션·입력 처리·에디터 프리뷰)

### A1 ★ AI MoveTo의 도착 판정 반지름
`Runtime/AIModule/Private/Navigation/PathFollowingComponent.cpp:1328` · `:136` · `:159` · `:125` / `AISystem.cpp:28` / `Tasks/AITask_MoveTo.cpp:21`

- 설명할 것 — `UseRadius` 식의 항 셋이 각각 무엇인지. `MinAgentRadiusPct = 1.1f`가 왜 붙는지. `DefaultAcceptanceRadius = -1.f`의 의미. `bFinishMoveOnGoalOverlap`이 어디서 흘러오는지
- 예상 질문 — *"그럼 수용 반지름을 0으로 주면 어떻게 됩니까?"* / *"래턴트 노드가 뭡니까?"*
- **가장 중요하다.** 이 한 건이 "엔진을 읽을 줄 안다"의 대표 사례가 된다

### A5 ★ 배열 범위 초과 시 동작
`KismetArrayLibrary.cpp` — `GenericArray_Get`

- 설명할 것 — 왜 크래시가 아니라 경고 + 기본값인지. 블루프린트의 안전 설계 철학. `FName`의 기본값이 무엇이고 왜 어떤 행 이름과도 안 맞는지
- 예상 질문 — *"C++였으면 어떻게 됐을까요?"* (`TArray::operator[]`는 체크 후 크래시 — 이 대비를 말할 수 있으면 강하다)

### A4 ★ 새 액터가 저장되지 않는 이유
`LevelEditorActions.cpp:4367` · `:568` → `FileHelpers.cpp:4444-4453`

- 설명할 것 — 패키지(`UPackage`)가 무엇인지. `IsDirty()`와 `PKG_NewlyCreated`의 차이. One File Per Actor에서 액터가 왜 각자 패키지를 갖는지
- 예상 질문 — *"World Partition / OFPA가 뭡니까?"*

### A8 ★ UHT의 자동 `BlueprintPure`
`Programs/Shared/EpicGames.UHT/Types/UhtFunction.cs:707`

- 설명할 것 — UnrealHeaderTool이 무엇을 하는 도구인지, 왜 C#으로 작성돼 있는지. `BlueprintCallable`과 `BlueprintPure`의 차이(실행 핀 유무, 호출 시점, 재계산)
- 예상 질문 — *"pure 노드를 여러 곳에 연결하면 어떻게 됩니까?"* — **이 프로젝트에서 실제로 겪은 버그다**(`TryConsumeSelected`의 pure 재계산). 사례로 답할 수 있으면 가장 좋다

---

## 나머지 아홉

### A2 `Expose on Spawn`의 전제 조건
`Editor/BlueprintGraph/Private/K2Node_ConstructObjectFromClass.cpp:133-140`

- 설명할 것 — `bIsSettableExternally = !CPF_DisableEditOnInstance`. Instance Editable이 꺼져 있으면 Expose on Spawn만 켜도 핀이 안 나온다
- 예상 질문 — *"`CPF_` 플래그가 뭡니까?"*

### A3 `Get Data Table Row`의 핀 조건
`K2Node_GetDataTableRow.cpp:145`

- 설명할 것 — `DefaultObject != nullptr && LinkedTo.Num() == 0`일 때만 행 구조체를 알아낸다. 선을 연결하면 `Out Row`가 와일드카드로 남아 컴파일 실패
- 예상 질문 — *"그럼 DataTable을 런타임에 바꾸려면요?"*

### A12 `TraceTypeQuery1`이 무슨 채널인가
`CollisionProfile.cpp:373-377` + `Config/DefaultEngine.ini`

- 설명할 것 — 채널을 오름차순으로 돌며 `ECC_Visibility`(3)를 `ECC_Camera`(4)보다 먼저 매핑한다. **커스텀 채널을 선언하면 인덱스가 밀린다**
- 예상 질문 — *"콜리전 채널과 프로파일의 차이는?"*

### A7 컴포넌트 소켓 이름을 외부에서 못 쓰는 이유
`Runtime/Engine/Classes/Components/SceneComponent.h:112` · `Classes/Engine/SCS_Node.h:44`

- 설명할 것 — `AttachSocketName`과 `AttachToName`이 각각 무엇인지. SCS(Simple Construction Script)가 무엇인지
- 예상 질문 — *"블루프린트 컴포넌트는 언제 만들어집니까?"*

### A9 콘텐츠 팩이 메뉴에 안 뜨는 이유
`Editor/AddContentDialog/Private/ViewModels/AddContentWidgetViewModel.cpp:130-133`

- 설명할 것 — `SharedPack` 카테고리가 목록 생성 시점에 필터링된다
- 낮은 우선순위. **"메뉴에 없더라"를 "코드가 거른다"로 바꾼 사례**로만 기억하면 된다

### A10 캡슐 트레이스에 회전 인자가 없다
`KismetSystemLibrary.h`

- 설명할 것 — `CapsuleTraceMultiForObjects`에 `Orientation` 인자가 없어서 캡슐 축을 칼날 방향으로 못 돌린다. 그래서 박스 트레이스로 갔다
- 예상 질문 — *"스윕 트레이스와 오버랩 판정의 차이는?"* / *"터널링이 뭡니까?"*
- **자기 사양의 오류를 소스로 잡은 사례.** A8과 짝으로 쓸 수 있다

### A11 노티파이 윈도우의 브로드캐스트
`AnimNotify_PlayMontageNotify.cpp`

- 설명할 것 — 윈도우가 `Begin`과 `End`를 모두 쏜다. 몽타주 노티파이와 노티파이 스테이트의 차이
- 예상 질문 — *"타격 판정을 왜 노티파이로 합니까?"* (애니메이션과 판정 프레임을 묶기 위해)

### A13 가속도 0일 때의 회전
`ComputeOrientToMovementRotation` (`CharacterMovementComponent`)

- 설명할 것 — 가속도가 0이면 현재 회전을 그대로 반환한다 → 멈춰 있으면 방향이 안 바뀐다
- 낮은 우선순위

### A14 `Parent Socket` 버튼이 무반응인 이유
`OnBrowseSocket`

- 설명할 것 — 부모의 `HasAnySockets()`가 거짓이면 조용히 리턴한다. **UI 버그가 아니라 조건 미충족**
- 낮은 우선순위

---

## 쓰지 않기로 한 것 — 표시해둔다

- **`AnimNode_LayeredBoneBlend.cpp:155, 181-192, 205`** — 상하체 분리가 루트 모션을 죽이는지.
  **소스로 판단했고 PIE에서 확인하지 않았다.** 기록에 그렇게 적혀 있다. 물어보면 "소스로는 그렇게
  읽었고 실제로 확인하지는 않았다"고 답한다. **되는 척하지 않는다**
- **`Knife.h` · `Potion_Small.h`** — 파일이 아니다. DataTable 행 이름이 검색에 걸린 오탐.
  개수를 셀 때 빼야 한다

## 값·동작 확인만 한 것 일곱 (예비)

`GameplayStatics.h:64-70` · `KismetTraceUtils.cpp:125-136`(디버그 선 색) ·
`LocalLightComponent.cpp:13-16`(`Intensity 5000`이 엔진 기본값) ·
`K2Node_SwitchEnum.cpp`(핀 순서 = 선언 순서) · `InputMappingContext.h`(필드 deprecated) ·
`SAnimNotifyPanel.cpp`(메뉴 문구) · `KismetMathLibrary.inl:490`

면접에서 "더 있냐"고 물으면 꺼낸다. 포트폴리오에는 안 넣는다.
