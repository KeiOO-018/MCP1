# CLAUDE.md — MCP1

## 프로젝트

- UE 5.8 / ThirdPerson 템플릿 기반 / **순수 블루프린트 프로젝트 (Source 없음)**
- 성격: **연습장.** AI 활용 실험과 복습용. 망가져도 재생성 가능.
- MCP 서버: `unreal-mcp` (HTTP `127.0.0.1:8000/mcp`).
  **에디터를 켜둔 채 라이브로 조작하는 방식**이 이 프로젝트의 기본 작업 형태다.

---

## 세션 규칙

### 1. 작업 전에 커밋한다

MCP로 무언가 만들기 전에 먼저 커밋한다(또는 사용자에게 커밋을 제안한다).
그러면 작업 후 `git status` 자체가 "무엇을 건드렸는가"의 증거가 된다.

### 2. 구현 전에 사양과 합격 기준을 못 박는다

**만들기 전에 아래를 확정한다.** 프로젝트를 먼저 뒤져 알 수 있는 것은 조사하고, 남은 것만 묻는다.
**질문은 한 번에 모아서 한다** — 나눠 묻지 않는다.

- **네트워킹**: 싱글 / 멀티. 멀티면 무엇을 리플리케이트하고 어디까지 서버 권한인가.
  나중에 얹는 비용이 가장 크므로 반드시 지금 정한다
- **상태와 전이**: 가질 수 있는 상태와 전이 조건. 하나뿐이면 그렇게 적는다
- **충돌**: 콜리전 프리셋/채널, Overlap인지 Hit인지
- **물리**: 시뮬레이션 여부, 질량·마찰
- **애니메이션**: 몽타주 / 애님 BP / 없음, 재생 트리거
- **틱**: 매 프레임이 정말 필요한가. 기본은 틱을 끄고 타이머·이벤트로 간다
- **튜닝 값의 소유자**: 하드코딩 / 인스턴스 편집 가능 변수 / DataAsset / DataTable
- **수명**: 스폰·디스폰 주체, `EndPlay`에서 정리할 것

그 다음 `PIE에서 ___하면 ___가 된다` 형태로 합격 기준 2~4개를 제시하고 합의한다.
검증은 이 기준으로만 한다. **확인하지 못한 항목은 반드시 밝힌다** — 되는 척하지 않는다.

사용자가 "알아서 해달라"고 해도 **항목별로 안을 제시하고 승인받은 뒤** 진행한다. 임의로 정하지 않는다.

### 3. 작업이 끝나면 기록을 먼저 제안한다

사용자가 말하지 않아도 **"`Docs/AI-Log`에 기록 남길까요?"를 먼저 물어본다.**

- `Docs/AI-Log/_template.md` 양식대로 작성하고, `Docs/AI-Log/README.md` 세션 목록에 한 줄 추가
- 사용자가 입력한 **명령 원문을 다듬지 말고 그대로** 보존한다
- **5장(몰랐던 것) · 7장(내 결정) · 8장(혼자 할 수 있는가)은 비워둔다.**
  사용자가 직접 채우는 칸이다. AI가 채우면 의미가 없어진다.

---

## 애셋 취급

- `.uasset` / `.umap`은 바이너리다. **텍스트로 열거나 편집하지 않는다.**
- 단, **MCP를 통한 에디터 조작은 허용된다.** 이 프로젝트의 주 작업 방식이다.
- 애셋을 수정하면 무엇을 건드렸는지 명시하고,
  **기존 노드·입력 매핑이 보존됐는지 확인한 뒤 보고한다.**
- 애셋 삭제·이동은 하지 않는다. 필요하다고 판단되면 먼저 묻는다.

---

## 블루프린트 작업

### 틱 예산

`Event Tick`과 거기서 불리는 모든 것은 프레임 예산을 쓴다. 최적화가 아니라 정확성 문제다.

- 기본은 **Class Defaults의 `Start with Tick Enabled`를 끈다.** 타이머·델리게이트·이벤트로 대체한다
- 매 틱 `Get All Actors Of Class` / `Find Component By Class` 금지.
  `BeginPlay`에서 한 번 구해 변수에 캐시한다
- `Cast` 결과도 캐시한다. 매 프레임 캐스팅하지 않는다
- 틱에서 문자열 조립·`Print String` 금지
- 식별자는 `Name`, 화면에 보이는 문구는 `Text`(로컬라이제이션), 가공할 때만 `String`.
  `Text`를 `String`으로 내리지 않는다

### 네트워킹

- 리플리케이션을 **임의로 추가하거나 제거하지 않는다.** 네트워크 계약이 바뀐다
- 권한이 필요한 로직은 `Has Authority`로 감싼다
- 변수에 `Replicated`를 켰으면 `RepNotify`가 필요한지 함께 판단한다.
  값이 바뀔 때 반응이 따라와야 하면 `Replicated`만으로는 부족하다

### 조용히 깨지는 것들

- **부모 BP의 기본값을 바꿔도, 이미 그 값을 덮어쓴 자식 BP나 레벨에 배치된 인스턴스는 따라오지 않는다.**
  기본값을 고쳤다면 반드시 그 사실을 말한다
- **BP 변수·함수·이벤트 디스패처의 이름을 바꾸면** 그것을 참조하던 다른 BP·레벨 BP·애님 BP가
  컴파일 에러 없이 조용히 끊길 수 있다. 리네임이 필요하면 먼저 묻는다

---

## 하지 말 것

- 요청하지 않은 리팩터링, 요청 범위 밖의 기능 추가
- 튜닝 값(속도·쿨다운·데미지·커브·DataTable 행) 임의 변경 — 손으로 맞춘 값이다
- 추측으로 고치고 다시 돌려보기.
  **가설과 "그게 틀렸다면 무엇이 보일지"를 먼저 말하고**, PIE에서 재현해 로그를 읽는다
- 빌드·쿡·에디터 실행을 임의로 돌리기 (오래 걸린다)

---

## 스타일

- 최소 diff. 기존 구조와 명명을 따른다
- 변경마다 **왜** 그렇게 했는지 한 줄로 밝힌다
- 구조적 문제는 지적하되, 시키지 않으면 고치지 않는다
- 새로 만들기 전에 **엔진이 이미 주는 것을 먼저 찾는다** — 타이머, `Gameplay Statics`,
  델리게이트, 기존 컴포넌트. 지우는 것도 유효한 답이다
- 지금 요구사항이 정당화하지 않는 추상화를 만들지 않는다. 가상의 미래를 위한
  인터페이스·컴포넌트 레이어는 넣지 않는다
- 비슷한 것을 만들 때는 **이 프로젝트 안에서** 유사 사례를 먼저 찾아 그 패턴을 따른다.
  없을 때만 외부를 참고하되 웹 검색은 최대 1회

---

## 다른 프로젝트

`ICIProject` (`D:\2025\ProjectICI...`)는 **실전 포트폴리오 프로젝트**다.
거기서 작업할 때는 위 규칙 1(작업 전 커밋)이 선택이 아니라 **필수**다.
