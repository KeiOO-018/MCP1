# CLAUDE.md — MCP1

## 이 프로젝트가 무엇인가

- **UE 5.8**, ThirdPerson 템플릿 기반. **순수 블루프린트 — C++ Source 없음.**
- 활성 플러그인: `ModelContextProtocol`, `GameplayStateTree`, `EditorToolset`, `Terminal`, `ModelingToolsEditorMode`
- MCP 서버 `unreal-mcp` (`http://127.0.0.1:8000/mcp`).
  **에디터를 켜둔 채 라이브로 조작하는 것이 이 프로젝트의 기본 작업 방식이다.**
- 성격: 연습장. AI 활용 실험용. 망가져도 다시 만들 수 있다.

### unreal-mcp는 영어만 받는다

사용자는 한글로 지시하지만 **MCP에 보내는 명령은 영어로 옮겨서 보낸다.**
따라서 **한글 지시와 실제로 보낸 영어 명령은 서로 다른 두 개의 기록 대상이다.**

- 보낸 영어 원문을 **그대로** 보존한다. 나중에 읽기 좋으라고 다듬지 않는다.
  다듬는 순간 기록에 적힌 명령과 실제로 실행된 명령이 달라져 재현이 불가능해진다
- 한글 지시가 모호해서 영어로 옮기며 해석을 넣었다면, **그 해석을 말한다.**
  옮기는 과정에서 의미가 굳어진 것이지 사용자가 그렇게 말한 게 아니다

### 이 플러그인은 실험적이다

`unreal-mcp`의 응답은 **실제 에디터 상태와 다를 수 있다.**
"만들었다"는 응답이 곧 만들어졌다는 뜻이 아니다.

- MCP 응답을 성공 근거로 삼지 않는다. **에디터에서 실제 상태를 다시 확인한다**
- 응답과 실제가 어긋나면 **어긋났다는 사실 자체를 기록한다.** 이 프로젝트에서
  그건 버그가 아니라 관찰 결과다
- **실패·경고·어긋난 응답은 그 자리에서 원문을 확보한다.** 터미널 출력은 세션이
  끝나면 사라진다. 기록을 나중에 쓰더라도 이것만은 미루지 않는다
- 터미널 출력은 **영어가 원본이다.** 기록에 옮길 때 번역본만 남기지 않는다.
  번역은 깨진 출력을 매끄럽게 다듬어서, 잡으려던 어긋남을 지운다

---

# 이 세션이 주로 하는 일

**사용자가 UE의 Terminal에 직접 넣을 명령을 정리하는 것**이 기본 역할이다.
구현을 대신 하는 게 아니라, 한글로 말한 의도를 **그대로 복사해 붙일 수 있는 영어 명령**으로 만든다.

- 명령은 **복사 가능한 코드 블록으로 따로 낸다.** 설명 문단 안에 섞어 쓰지 않는다
- **한 번에 하나씩** 낸다. 여러 개가 필요하면 순서를 매기되,
  앞 명령의 결과를 보고 다음으로 간다. 미리 다 뽑아두지 않는다
- **명령마다 보고서를 파일로 받는다.** 모든 명령 끝에 이 줄을 넣는다.

  ```
  Write the report to Docs/Terminal-Log/YYYY-MM-DD-NN-slug.md
  (a repo path - do NOT write under Saved/, it is gitignored).
  ```

  `NN`은 `Docs/Terminal-Log/`에 있는 마지막 번호의 다음이다. **세션마다 리셋하지 않고
  계속 이어 센다.** 화면에 찍게 하면 터미널 폭에서 잘리고, **잘린 조각이 원문 행세를 한다.**
  2026-08-29에 정한 방식인데 이 파일에 안 적혀 있어서 08-31부터 09-03까지
  **다섯 세션 동안 조용히 사라졌었다.** 그 다섯 세션의 기록은 전부 "터미널 원문을
  확보 못 했다"고만 적혀 있고, 방법이 이미 있었다는 사실은 한 번도 안 적혔다
- **옮기며 정한 값은 반드시 밝힌다.** 한글 의도에 없던 숫자·플래그·이름을
  영어 명령에 넣었다면 그건 사용자가 말한 게 아니라 내가 정한 것이다
  (예: "각도 제한" → `±60`)
- 명령을 쓰기 전에 아래 1·2단계(심문·사양)를 먼저 지난다.
  **무엇을 만들지 안 정하고 명령부터 쓰지 않는다**

그 외 작업 — 직접 MCP 조작, 애셋 정리, 문서 작성 등 — 도 요청하면 한다.

---

# 작업 파이프라인

구현이 따르는 작업은 이 순서를 지난다.

```
심문 → 사양 → 명령 또는 구현 → 남의 눈 검증 → (기록)
```

1~4단계는 게이트다. 앞 단계의 산출물이 없으면 다음으로 가지 않는다.
**5단계 기록은 게이트가 아니라 선택이다** — 사용자가 요청할 때만 쓴다.

---

## 1단계 · 심문

구현 전에 **모르는 것을 전부 한 번에 몰아서 묻는다.** 나눠 묻지 않는다.

묻기 전에 먼저 조사한다. 프로젝트를 뒤져서 알 수 있는 것은 질문 목록에서 뺀다.
"Content 안에 이미 있나", "입력 액션이 이미 매핑돼 있나"는 물을 게 아니라 확인할 것이다.

질문은 **답이 갈리면 결과물이 달라지는 것**만 한다. 관례적 기본값이 있는 것은 그냥 정하고 말한다.

사용자가 "알아서 해줘"라고 해도 **항목별 안을 제시하고 승인받은 뒤** 진행한다.
임의로 정하고 넘어가지 않는다.

---

## 2단계 · 사양

심문의 답을 문장으로 고정한다. 최소한 아래가 정해져야 구현에 들어간다.

- **범위** — 이번에 만드는 것과 만들지 않는 것
- **상태** — 가질 수 있는 상태와 전이 조건. 하나뿐이면 "하나뿐"이라고 적는다
- **권한** — 싱글인가 멀티인가. 멀티면 무엇을 리플리케이트하고 어디까지 서버 권한인가
- **수명** — 누가 스폰하고 누가 정리하는가
- **튜닝 값의 주인** — 하드코딩인가, 인스턴스 편집 변수인가, DataAsset인가

이어서 **합격 기준**을 2~4개 쓴다. 형식은 하나뿐이다.

> `PIE에서 ___하면 ___가 된다.`

검증은 이 문장들로만 한다. 여기 없는 것은 "확인 안 됨"이다.

**사양은 `Docs/Spec/YYYY-MM-DD-슬러그.md`에 쓴다.** 대화에만 두지 않는다.
합격 기준은 "무엇으로 검증했는가"의 근거인데 **AI-Log에는 그걸 적는 칸이 없다** —
`_template.md`에 사양도 합격 기준도 없고 `확인한 것`은 결과만 적는다. 파일로 안 남기면
세션이 끝나는 순간 기준 문장이 사라지고, 나중에 "무엇을 통과로 쳤는지"를 알 수 없다.

`Docs/AI-Log/_template.md`와 달리 **사양에는 고정 양식이 없다.** 위 다섯 항목
(범위 · 상태 · 권한 · 수명 · 튜닝 값의 주인)과 합격 기준만 있으면 되고, 나머지는
그 작업에 필요한 만큼 쓴다. 기존 파일들의 섹션 수가 8~16개로 제각각인 것이 그 뜻이다.

이 규칙은 2026-09-03에 넣었다. `Docs/Spec/`은 2026-08-28부터 쓰이다가
**2026-09-01 이후 다섯 세션 넘게 끊겼는데, 이 파일에 적혀 있지 않아서였다.**
`Docs/Terminal-Log/`가 같은 이유로 사라졌던 것과 같은 실패다.

---

## 3단계 · 명령 또는 구현 — 결정 사다리

**명령을 쓸 때도 이 사다리를 먼저 지난다.** 내가 만들든 사용자가 Terminal로 만들든,
안 만들어도 되는 것을 만들라고 시키는 명령이 가장 비싸다.

새 노드·새 변수·새 블루프린트를 만들기 전에 **위에서부터 차례로** 묻는다.
어느 칸에서든 답이 나오면 **거기서 멈춘다.** 아래로 내려갈수록 비싸다.

1. **안 만들어도 되는가?** — 요구사항이 정말 이걸 요구하는가. 아니면 넘어간다
2. **이 프로젝트에 이미 있는가?** — `Content/` 안에서 먼저 찾는다. 있으면 그걸 쓴다
3. **엔진이 이미 주는가?** — Timer, Delegate, `GameplayStatics`, Enhanced Input,
   Character Movement의 기본 변수. 대부분 여기서 끝난다
4. **켜둔 플러그인이 주는가?** — 상태 기계가 필요하면 `GameplayStateTree`가 이미 있다
5. **노드 하나로 되는가?** — 되면 하나로 만든다
6. **여기까지 왔으면** 요구사항이 지금 정당화하는 만큼만 만든다

**깎지 않는 것** — 사다리를 이유로 이것들을 빼지 않는다.
Cast 실패 분기, 널 체크, `Has Authority` 권한 검사, `EndPlay` 정리, 입력 매핑 보존.

**가상의 미래를 위한 구조를 만들지 않는다.** 지금 하나뿐인데 인터페이스를 파거나
컴포넌트로 분리하지 않는다. 두 번째가 실제로 생겼을 때 그때 뽑는다.

**지우는 것도 유효한 답이다.**

---

## 4단계 · 남의 눈 검증

**만든 사람의 머리로 검증하지 않는다.** 구현하면서 "이건 되겠지"라고 생각한 것은
검증 대상이 아니라 검증에서 가장 먼저 빠지는 것이다.

만든 직후, 구현 과정을 잊었다고 치고 다시 본다.

- 2단계의 합격 기준을 **하나씩** PIE에서 확인한다
- 확인한 것과 **확인 못 한 것을 나눠서 보고한다.** 되는 척하지 않는다
- 무엇을 건드렸는지 `git status`로 확인하고 명시한다
- 부모 BP 기본값을 바꿨다면 **그 값을 이미 덮어쓴 자식·레벨 인스턴스는 따라오지 않는다.**
  그 사실을 반드시 말한다

**버그를 만났을 때** — 고쳐보고 돌려보는 것을 반복하지 않는다.

1. 가설을 한 문장으로 말한다
2. **"이 가설이 틀렸다면 무엇이 보일지"를 먼저 말한다**
3. PIE에서 재현해 로그를 읽는다
4. 그 다음에 고친다

증거 없이 고친 것은 고친 게 아니다.

---

## 5단계 · 기록

**기록은 매 세션의 의무가 아니다. 사용자가 요청할 때만 쓴다.**
작업이 끝날 때마다 기록할지 묻지 않는다 — 그건 방해다.

다만 **기록해두면 아까울 것이 나왔을 때는 한 번 짚는다.** 예를 들어
MCP 응답과 실제 상태가 어긋난 사례, 예상 밖의 부작용, 되돌리기 어려운 결정.
짚기만 하고 사용자가 답하지 않으면 넘어간다.

### 양식은 사용자가 정한다

**`Docs/AI-Log/_template.md`가 기록 양식의 유일한 근거다.**
이 CLAUDE.md는 무엇을 적을지 정하지 않는다. 양식 파일이 곧 규칙이다.

- 기록을 쓰기 전에 **매번 `_template.md`를 다시 읽는다.** 기억에 의존하지 않는다
- **섹션을 추가하지 않는다.** 양식에 없는 항목은 유용해 보여도 넣지 않는다
- **섹션을 빼거나 순서를 바꾸지 않는다.** 제목 문구도 양식 그대로 쓴다
- 양식이 바뀌면 그때부터 바뀐 양식을 따른다. 지난 기록을 소급해서 고치지 않는다
- 양식 안의 주석(`<!-- -->`)은 **작성 지시로 읽고 결과물에는 남기지 않는다**
- `_template.md`가 없거나 비어 있으면 **기록을 쓰지 않고 사용자에게 양식을 요청한다.**
  임의로 양식을 만들어 채우지 않는다

### 채우는 방식

- **빈칸을 남기지 않는다.** 양식이 명시적으로 사용자 몫이라고 표시한 칸만 비운다
- 사용자의 **명령 원문은 다듬지 말고 그대로** 옮긴다
- 기록의 독자는 **그 자리에 없던 사람**이다. 나중의 본인도 포함된다.
  따라서 기록에는 **압축을 적용하지 않는다.** 여기서만은 길게 쓴다
- 확인 못 한 것은 확인 못 했다고 적는다. 되는 척하지 않는다

### 인계

기록은 띄엄띄엄 남으므로 **직전 세션의 기록이 아니라 가장 최근 기록**을 본다.
`Docs/AI-Log`에 기록이 있으면 세션을 시작할 때 **가장 최근 것의 `다음으로 넘김`을 읽고**
사용자에게 짚어준다. 이어서 할 일인지 접어둘 일인지 확인한다.

처리한 항목은 그 기록에서 지운다. 안 지우면 목록이 쌓여서 아무도 안 읽는다.
기록이 하나도 없으면 그냥 시작한다.

---

# 애셋 취급

- `.uasset` / `.umap`은 **바이너리다. 텍스트로 열거나 편집하지 않는다.**
- 단 **MCP를 통한 에디터 조작은 허용된다.** 이게 주 작업 방식이다.
- **삭제·이동·리네임은 하지 않는다.** 필요해 보이면 먼저 묻는다.
  BP 변수·함수·이벤트 디스패처 이름을 바꾸면 그걸 참조하던 다른 BP가
  **컴파일 에러 없이 조용히 끊긴다.**
- 작업 전 커밋한다. 그래야 작업 후 `git status`가 "무엇을 건드렸는가"의 증거가 된다.

---

# 출력 방식

- **짧게 쓴다.** 서론·요약·"~를 해보겠습니다" 없이 결과부터.
- 변경마다 **왜** 그렇게 했는지 한 줄. 그 이상은 묻기 전까지 쓰지 않는다.
- **예외 — 아래는 압축하지 않는다.** 사양, 합격 기준, 검증 결과, AI-Log 기록,
  확인 못 한 것의 목록. 이것들은 짧게 쓰면 값을 잃는다.
- **코드 블록은 검증한 것만 넣는다.** 이 프로젝트에서 코드 블록은 "그대로 복사해
  붙이라"는 신호다. 확인 안 한 UI 경로·메뉴 위치·버전별 동작을 코드 블록에 넣으면
  검증한 것과 화면에서 구분이 안 된다. 미검증은 산문으로 쓰고 **"확인 안 함"을 붙인다.**
  엔진 소스가 `C:\Program Files\Epic Games\UE_5.8\Engine\Source`에 통째로 있으니
  추측하지 말고 확인한다 — 전체 grep은 120초를 넘기므로 `Source/Editor/<모듈>`로 좁힌다.
- **정정할 때 새로운 추측을 덧붙이지 않는다.** 왜 틀렸는지 모르면 "사용자 말이 맞다"에서
  멈춘다. 그럴듯한 내력을 지어 붙이면 틀린 것이 정리된 것처럼 보인다.

---

# 하지 말 것

- 요청하지 않은 리팩터링, 요청 범위 밖의 기능 추가
- 튜닝 값(속도·쿨다운·데미지·커브·DataTable 행) 임의 변경 — 손으로 맞춘 값이다
- 리플리케이션을 임의로 추가하거나 제거하기 — 네트워크 계약이 바뀐다
- `Event Tick`에서 `Get All Actors Of Class`, `Find Component By Class`, `Cast`, 문자열 조립.
  `BeginPlay`에서 한 번 구해 캐시한다. 기본은 **Tick을 꺼둔다**
- 빌드·쿡·에디터 실행을 임의로 돌리기 — 오래 걸린다. 필요하면 묻는다
- 웹 검색 — 이 프로젝트 안에서 유사 사례를 먼저 찾는다. 없을 때만, 최대 1회
