# CLAUDE.md — MCP1

## 프로젝트

- UE 5.8 / ThirdPerson 템플릿 기반 / **순수 블루프린트 프로젝트 (Source 없음)**
- 성격: **연습장.** AI 활용 실험과 복습용. 망가져도 재생성 가능.
- MCP 서버: `unreal-mcp` (HTTP `127.0.0.1:8000/mcp`).
  **에디터를 켜둔 채 라이브로 조작하는 방식**이 이 프로젝트의 기본 작업 형태다.

---

## 세션 규칙

### 1. 작업 전에 커밋한다

MCP로 무언가 만들기 전에 먼저 커밋한다(또는 사용자에게 커밋을 제안한다).
그러면 작업 후 `git status` 자체가 "무엇을 건드렸는가"의 증거가 된다.

### 2. 구현 전에 합격 기준을 못 박는다

`PIE에서 ___하면 ___가 된다` 형태로 2~4개를 먼저 제시하고 합의한다.
검증은 이 기준으로만 한다. **확인하지 못한 항목은 반드시 밝힌다** — 되는 척하지 않는다.

### 3. 작업이 끝나면 기록을 먼저 제안한다

사용자가 말하지 않아도 **"`Docs/AI-Log`에 기록 남길까요?"를 먼저 물어본다.**

- `Docs/AI-Log/_template.md` 양식대로 작성하고, `Docs/AI-Log/README.md` 세션 목록에 한 줄 추가
- 사용자가 입력한 **명령 원문을 다듬지 말고 그대로** 보존한다
- **5장(몰랐던 것) · 7장(내 결정) · 8장(혼자 할 수 있는가)은 비워둔다.**
  사용자가 직접 채우는 칸이다. AI가 채우면 의미가 없어진다.

---

## 애셋 취급

- `.uasset` / `.umap`은 바이너리다. **텍스트로 열거나 편집하지 않는다.**
- 단, **MCP를 통한 에디터 조작은 허용된다.** 이 프로젝트의 주 작업 방식이다.
- 애셋을 수정하면 무엇을 건드렸는지 명시하고,
  **기존 노드·입력 매핑이 보존됐는지 확인한 뒤 보고한다.**
- 애셋 삭제·이동은 하지 않는다. 필요하다고 판단되면 먼저 묻는다.

---

## 하지 말 것

- 요청하지 않은 리팩터링, 요청 범위 밖의 기능 추가
- 튜닝 값(속도·쿨다운·데미지·커브·DataTable 행) 임의 변경 — 손으로 맞춘 값이다
- 추측으로 고치고 다시 돌려보기. PIE에서 재현하고 로그를 읽는다
- 빌드·쿡·에디터 실행을 임의로 돌리기 (오래 걸린다)

---

## 스타일

- 최소 diff. 기존 구조와 명명을 따른다
- 변경마다 **왜** 그렇게 했는지 한 줄로 밝힌다
- 구조적 문제는 지적하되, 시키지 않으면 고치지 않는다

---

## 다른 프로젝트

`ICIProject` (`D:\2025\ProjectICI...`)는 **실전 포트폴리오 프로젝트**다.
거기서 작업할 때는 위 규칙 1(작업 전 커밋)이 선택이 아니라 **필수**다.
