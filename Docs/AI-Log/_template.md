<!--
Docs/AI-Log 기록 양식. 이 파일이 양식의 유일한 근거다.
섹션을 추가·삭제하거나 순서를 바꾸지 않는다. 제목 문구도 이대로 쓴다.
파일명: YYYY-MM-DD-작업물slug.md   (예: 2026-08-28-camera-switch.md)
이 주석 블록과 각 섹션의 주석 지시문은 작성 지시일 뿐, 결과물에 남기지 않는다.
-->

# YYYY-MM-DD

## 작업물

<!-- 무엇을 만들었는가. 한 줄. 산출물 목록은 여기가 아니라 분석 첫 칸에 적는다. -->

**소요 시간**:

## 명령

### 한글

<!-- 사용자가 입력한 원문. 오타·말투 그대로. 다듬지 않는다.
     여러 번 지시했으면 보낸 순서대로 전부. -->

### English — MCP에 실제로 보낸 명령

<!-- unreal-mcp에 들어간 영어 원문 그대로. 읽기 좋게 고치지 않는다.
     한글 지시를 영어로 옮기며 해석을 넣었다면 그 해석을 여기 밝힌다. -->

## Terminal 결과

<!-- 터미널 출력은 영어가 원본이다. 번역본만 남기지 않는다.
     번역은 어긋나거나 깨진 출력을 매끄럽게 다듬어버려서,
     분석 칸이 잡으려는 신호를 옮기는 과정에서 지운다. -->

### 원문 — English

<!-- 터미널이 뱉은 그대로. 문장을 고치지 않는다.
     길면 판단 근거가 된 줄만 발췌하되, 발췌했다는 사실을 밝힌다.
     실패 · 경고 · 어긋난 응답은 발췌하지 말고 전문을 붙인다. -->

### 요약 — 한글

<!-- 번역이 아니라 요약이다. 무엇이 바뀌었는가 — 에셋 · 노드 · 변수 단위로.
     식별자는 번역하지 않는다. 클래스명 · 함수명 · 변수명 · 플래그 · 에러 문구는
     영어 그대로 둔다. 옮기면 검색도 엔진 문서 대조도 안 된다. -->

## 분석

<!-- 만든 사람의 머리로 쓰지 않는다. 결과물을 처음 보는 사람의 눈으로 다시 본다. -->

### 무엇을 만들었나

<!-- 산출물을 덩어리로 나눠 적는다. 예: 입력 / 컴포넌트 / 변수 / 함수.
     각 덩어리마다 실제 값까지 — 위치, FOV, 플래그, 매핑 키. -->

### 기술적으로 맞게 짚은 부분

<!-- "무엇을 했다"가 아니라 "왜 그게 정석인가"를 적는다.
     엔진이 그렇게 동작하는 근거를 같이 댄다.
     안 한 판단이 옳았던 것도 포함한다 — 손대지 않은 게 정답인 경우가 있다. -->

### 확인한 것 / 확인 못 한 것

<!-- 두 줄로 나눠 적는다. 확인 못 한 것은 왜 못 했는지 이유까지.
     MCP 응답은 확인이 아니다. 에디터에서 실제로 본 것만 "확인한 것"이다.
     "다 됐다"로 뭉개지 않는다. -->

### 남는 리스크

<!-- 관찰된 것을 전부 적는다. 고칠지 말지는 여기서 정하지 않는다.
     동작하지만 티가 나는 것, 확장하면 깨지는 것, 트레이드오프로 고른 것. -->

### 총평

<!-- 요청 대비 충족 여부. 이 작업의 실질적 난이도가 무엇이었는지. -->

## AI의 제안

<!-- 사용자가 시키지 않았는데 AI가 먼저 꺼낸 것. 없으면 "없음".
     두 줄로 적는다.
     1) 제안 원문 — 인용으로. 영어로 나왔으면 영어 그대로, 그대로 복사해 MCP에 보낼 수 있게
     2) 해설 한 줄 — 이 제안이 무엇을 얻고 무엇을 잃는가.
        원문만 남기면 나중에 왜 그런 말이 나왔는지 알 수 없다.
     채택 여부는 여기 적지 않는다. 실제로 할 것이면 "다음으로 넘김"으로 옮긴다. -->

## 다음으로 넘김

<!-- 다음 세션이 이 칸부터 읽는다.
     남는 리스크 중 실제로 손댈 것만 옮긴다. 관찰 전부를 옮기지 않는다.
     결정이 필요한 것도 여기 적는다. 없으면 "없음". -->
