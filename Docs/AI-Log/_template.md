# [작업 이름]

> 이 기록의 목적: **무엇을 어떻게 만들었는지 남기고, 나중에 다시 확인하는 것.**
> 그때 자리에 없던 사람이 읽어도 이해되게 쓴다. AI가 전 항목을 채우고, 사용자가 틀린 곳을 고친다.
> 예외는 7장의 "내 결정" 칸 하나 — 거기만 사용자가 쓴다.

## 1. 메타

| 항목 | 값 |
|---|---|
| 날짜 | YYYY-MM-DD |
| 프로젝트 | |
| 엔진 | UE 5.x |
| MCP 서버 | |
| 소요 시간 | |
| MCP 호출 수 | |
| 직전 커밋 | `해시` (작업 전 기준점) |
| 결과 커밋 | `해시` |

**작업 방식:** (에디터를 켜둔 채 MCP로 조작했는지, 문서 작업인지 등 한두 줄)

---

## 2. 내가 내린 명령 (원문 그대로)

> 실제로 입력한 프롬프트를 **다듬지 말고** 붙여넣는다. 여러 번이면 순서대로 전부.

**요구사항:**
1.

**명시하지 않았는데 처리된 것:**
-

---

## 3. 변화점

작업 후 `git status --short` 결과:

```
```

| 상태 | 파일 | 내용 |
|---|---|---|
| 신규 | | |
| 수정 | | |

**변경 규모:** 전체 N개 파일 중 실질 수정 M + 신규 K

### 추가된 것 상세

> 컴포넌트·변수·함수·설정값을 표로. **값과 함께 그 값인 이유**를 적는다.

---

## 4. 구현 분석 — 왜 이게 동작하는가

> 무엇을 했는지가 아니라 **엔진이 왜 그렇게 반응하는지**를 쓴다.
> 이 기록에서 가장 값이 나가는 항목이다. 항목별로 소제목을 나눈다.

---

## 5. 이번 작업에 쓰인 엔진 지식

> 이 작업이 성립하려면 알아야 했던 **비자명한 것들.** 나중에 이 줄만 읽고 복기할 수 있게
> 한 항목당 한두 문장으로 쓴다. 없으면 "없음".

| 지식 | 내용 |
|---|---|
| | |

---

## 6. 검증 범위

**확인된 것**
- [x]

**확인 못 한 것 (중요 — 정직하게)**
- [ ]

---

## 7. 설계 판단과 대안

> 갈림길에서 무엇을 골랐고 무엇을 버렸는가. 트레이드오프까지 적는다.
> **"내 결정" 칸만 사용자 몫이다** — 대안 쪽으로 갈 생각이면 여기 적어둔다. 없으면 비운 채 둔다.

| 선택한 방식 | 대안 | 트레이드오프 | 선택 이유 | 내 결정 |
|---|---|---|---|---|
| | | | | |

### 알아둘 잠재 이슈

-

---

## 8. 재현 난이도 — 다시 만든다면 어디가 까다로운가

> 학습 자가진단이 아니라 **이 작업의 난이도가 어디에 있었는지**의 기록이다.
> 쉬운 부분과 함정을 구분해 적는다.

| 단계 | 난이도 | 까다로운 지점 |
|---|---|---|
| | 쉬움 / 보통 / 함정 | |

**이 작업의 핵심 한 줄:**

**다음에 시도해볼 것:**
- [ ]

---

## 9. 인수인계 — 다음 세션이 이어받을 것

> 다음 세션이 `CLAUDE.md` 규칙 0으로 이 절을 읽는다. **처리한 항목은 지운다.**
> 미룬 개선 / 확인 못 한 것 / 결정이 필요한 것. 없으면 "없음".

-
